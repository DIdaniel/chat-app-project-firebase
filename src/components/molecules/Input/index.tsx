import React from 'react';

type InputProps = {
  //
};

export const Input = (props: InputProps) => {
  /** Property */
  const {...others} = props;

  /** Function */

  /** Render */
  return <div className="bg-red-200 flex flex-1">Input</div>;
};
