import React from 'react';

type MessageProps = {
  //
};

export const Message = (props: MessageProps) => {
  /** Property */
  const {...others} = props;

  /** Function */

  /** Render */
  return <div>Message</div>;
};
