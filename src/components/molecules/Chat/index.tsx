import React from 'react';

type ChatProps = {
  //
};

export const Chat = (props: ChatProps) => {
  /** Property */
  const {...others} = props;

  /** Function */

  /** Render */
  return <div>Chat</div>;
};
